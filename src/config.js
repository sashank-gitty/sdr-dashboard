// The one place this dashboard's employer name lives. Change this when
// you move to a different company — the top-right identity badge is the
// only thing that reads it, and everything else in the app (signal data,
// territory book, competitor list) is already company-agnostic, so a
// single-line edit here is the whole "move to a new job" checklist for
// the UI itself.
export const ORG_NAME = "Qualtrics"
